---
const PROJECTS = [
  {
    title: "project 1",
    description: "Lorem Ipsum dolor sit amet",
    link: "#",
    github: "#",
    image: "",
    tags: ["HTML", "CSS", "JavaScript"],
  },
  {
    title: "project 2",
    description: "Lorem Ipsum",
    link: "#",
    github: "#",
    image: "",
    tags: ["HTML", "CSS", "JavaScript"],
  },
];
---

{
  PROJECTS.map(({ title, description, image, tags }) => (
    <article>
      <h3>{title}</h3>
      <p>{description}</p>
      <ul>
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
      <img src={image} alt={`Screenshot of the project ${image}`} />
    </article>
  ))
}
